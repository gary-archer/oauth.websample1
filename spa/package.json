{
  "name": "basicspa",
  "version": "1.0.0",
  "description": "A Basic SPA using the oidc-client security library",
  "main": "app.ts",
  "keywords": [],
  "author": "Gary Archer",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "lint": "npx eslint . --ignore-pattern dist",
    "start": "npm run clean && vite build --mode development --watch",
    "build": "npm run clean && vite build --mode production",
    "clean": "rm -rf dist && mkdir dist"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "mustache": "^4.2.0",
    "oidc-client-ts": "^3.4.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@types/mustache": "^4.2.5",
    "@types/node": "^24.10.3",
    "eslint": "^9.39.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.49.0",
    "vite": "^7.3.0"
  }
}
